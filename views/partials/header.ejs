<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= locals.title || 'CivicLogic' %> | CivicLogic</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        :root {
            --eci-blue: #003399;
            --eci-orange: #FF9933;
            --eci-green: #138808;
            --glass-bg: rgba(255, 255, 255, 0.8);
        }
        body { font-family: 'Outfit', sans-serif; }
        .font-serif { font-family: 'Playfair Display', serif; }
        .bg-eci-blue { background-color: var(--eci-blue); }
        .text-eci-blue { color: var(--eci-blue); }
        .border-eci-blue { border-color: var(--eci-blue); }
        
        .animate-fade-in { animation: fadeIn 0.5s ease-out forwards; }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .glass-panel {
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        /* News Styles */
        .news-newspaper {
            background: #fdfaf3;
            border: 1px solid #e5e0d5;
            box-shadow: 5px 5px 0px #e5e0d5;
            font-family: 'Playfair Display', serif;
        }
        .news-mobile {
            background: #111;
            color: #fff;
            border-radius: 2rem;
            border: 4px solid #333;
        }
        .news-website {
            background: #fff;
            border-top: 4px solid var(--eci-blue);
            box-shadow: 0 10px 30px -10px rgba(0,0,0,0.1);
        }
        .news-item-active {
            outline: 4px solid var(--eci-blue);
            transform: scale(1.02);
            z-index: 10;
        }
        /* Desktop hover lift for news cards */
        @media (hover: hover) {
            .news-newspaper:hover {
                transform: translateY(-6px) rotate(-1deg);
                box-shadow: 8px 12px 0px #d5cfc4;
                transition: transform 0.25s ease, box-shadow 0.25s ease;
            }
            .news-mobile:hover {
                transform: translateY(-6px) scale(1.03);
                box-shadow: 0 20px 40px -8px rgba(0,0,0,0.5);
                transition: transform 0.25s ease, box-shadow 0.25s ease;
            }
            .news-website:hover {
                transform: translateY(-6px);
                box-shadow: 0 20px 40px -8px rgba(0,51,153,0.15);
                transition: transform 0.25s ease, box-shadow 0.25s ease;
            }
        }
        /* Mobile: hide body text in news cards to reduce scroll */
        @media (max-width: 767px) {
            .news-body-text { display: none; }
        }

        /* ── LogicAnalogy Tooltip Component ──────────────────────────── */
        .logic-analogy {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            position: relative;
            cursor: help;
            border-bottom: 2px dashed var(--eci-orange);
            font-weight: 700;
        }
        .logic-analogy::before {
            content: '💡';
            font-size: 0.7rem;
        }
        .logic-analogy .analogy-bubble {
            display: none;
            position: absolute;
            bottom: calc(100% + 8px);
            left: 50%;
            transform: translateX(-50%);
            background: #1a1a2e;
            color: #fff;
            border-radius: 12px;
            padding: 10px 14px;
            font-size: 0.72rem;
            font-weight: 600;
            white-space: nowrap;
            z-index: 50;
            box-shadow: 0 8px 24px rgba(0,0,0,0.2);
            line-height: 1.4;
            text-align: center;
            min-width: 160px;
        }
        .logic-analogy .analogy-bubble::after {
            content: '';
            position: absolute;
            top: 100%;
            left: 50%;
            transform: translateX(-50%);
            border: 6px solid transparent;
            border-top-color: #1a1a2e;
        }
        .logic-analogy:hover .analogy-bubble {
            display: block;
            animation: fadeIn 0.2s ease-out;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
    <header class="bg-eci-blue text-white shadow-lg p-3 md:p-4">
        <div class="container mx-auto flex justify-between items-center gap-2">
            <!-- Full title on md+, short on mobile -->
            <h1 class="font-bold tracking-tight flex-shrink-0">
                <span class="hidden md:inline text-2xl">CivicLogic: The One-Vote Simulator</span>
                <span class="md:hidden text-lg">CivicLogic</span>
            </h1>

            <div class="flex items-center gap-2 md:gap-3">
                <!-- Demo badge: dot only on mobile, full text on md+ -->
                <div class="flex items-center gap-1.5 bg-white/10 border border-white/20 rounded-full px-2.5 py-1 md:px-4 md:py-1.5 text-xs font-semibold">
                    <span class="relative flex h-2 w-2 flex-shrink-0">
                        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-yellow-300 opacity-75"></span>
                        <span class="relative inline-flex rounded-full h-2 w-2 bg-yellow-400"></span>
                    </span>
                    <span class="hidden md:inline">Live Demo Preview</span>
                </div>

                <% if (locals.voterName) { %>
                    <div class="text-xs md:text-sm font-medium opacity-90 truncate max-w-[100px] md:max-w-none">
                        👤 <span class="font-bold"><%= locals.voterName %></span>
                    </div>
                <% } %>
            </div>
        </div>
    </header>

    <main class="flex-grow container mx-auto p-6">
